<%= simple_form_for pet do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :species, collection: Pet::SPECIES %>
  <%= f.input :found_on, as: :date, value: f.object.try(:strftime,"%m/%d/%Y") %>
  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>
